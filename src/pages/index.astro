---
import BaseLayout from '../layouts/BaseLayout.astro';
import HeroVideo from '../components/HeroVideo.astro';
import FluxBand from '../components/FluxBand.astro';
import { WHATSAPP_URL, DIAGNOSTIC_URL } from '../config';

// Soluções — formato da referência (Farda): nome + descrição. Descrições
// escritas na voz da Otto (direta, estratégica, "território", "patamar").
// Ordem em funil: diagnóstico → estratégia → construção → ativação.
const solucoes = [
  {
    title: 'Diagnóstico de marca',
    desc: 'O ponto de partida. Um raio-x do posicionamento que revela onde a marca perde valor e o que precisa mudar — antes de qualquer decisão visual.',
  },
  {
    title: 'Estratégia de marca',
    desc: 'Os fundamentos que orientam tudo: mercado, público, diferenciação e o território que a marca vai defender. A base de cada decisão.',
  },
  {
    title: 'Branding',
    desc: 'A construção da marca do princípio — estratégia, narrativa e identidade conectadas num sistema coeso, pronto para crescer com direção.',
  },
  {
    title: 'Rebranding',
    desc: 'Quando o negócio evolui, a percepção precisa acompanhar. Reposicionamos e reconstruímos a marca para refletir o que ela já é.',
  },
  {
    title: 'Identidade visual',
    desc: 'O rosto da marca. Logo, sistema gráfico e diretrizes que traduzem o posicionamento em algo reconhecível e consistente.',
  },
  {
    title: 'Web design',
    desc: 'O site como experiência de marca. Estrutura, narrativa e design que transformam posicionamento em percepção de valor e em oportunidade.',
  },
  {
    title: 'Motion',
    desc: 'Movimento a serviço da marca. Animação que amplia a percepção, apresenta conceitos e dá presença em cada canal.',
  },
  {
    title: 'Gestão de redes sociais',
    desc: 'Presença visual contínua. Direção criativa constante para manter a marca consistente e viva enquanto o negócio cresce.',
  },
];

// Método Otto — três patamares (copy real do doc). A numeração aqui É
// informação: patamar = degrau, e cada um precede o próximo.
const patamares = [
  { n: '01', name: 'Clareza', text: 'Quem a marca é, para quem existe, que espaço ela ocupa. Diagnóstico e posicionamento.' },
  { n: '02', name: 'Direção', text: 'As decisões que guiam tudo: plataforma de marca, estratégia verbal e visual.' },
  { n: '03', name: 'Sistema', text: 'A marca construída e consistente: identidade e aplicação.' },
];

// Cases — 1 modelo real (leva à página) + candidatos do doc como slots.
const cases = [
  { name: 'Página de case modelo', cat: 'Modelo', href: '/case-modelo', model: true },
  { name: 'Therapy and Beauty', cat: 'Estética · Bem-estar', model: false },
  { name: 'Astros Barbearia', cat: 'Barbearia', model: false },
  { name: 'Barreto Tour', cat: 'Turismo', model: false },
];

// Depoimentos — não existem ainda. Slots claramente marcados, em carrossel.
const depoimentos = [0, 1, 2, 3, 4];
---

<BaseLayout title="Otto Branding — Criamos marcas de Otto Patamar">
  <!-- ============================ HERO ============================ -->
  <section class="hero">
    <HeroVideo class="hero__flux" />
    <div class="hero__scrim" aria-hidden="true"></div>

    <div class="container hero__container">
      <div class="hero__content">
        <p class="eyebrow rise" style="--rise-delay: 60ms">Estratégia · Posicionamento · Identidade</p>
        <h1 class="hero__headline rise" style="--rise-delay: 140ms">
          <span class="hero__headline-quiet">A maioria das marcas compete por preço.</span>
          <span class="hero__headline-accent">As fortes constroem <em>preferência</em>.</span>
        </h1>
        <p class="hero__descritor rise" style="--rise-delay: 240ms">
          A Otto une estratégia e design para tirar sua marca do lugar comum e
          colocá-la em outro patamar de percepção de valor.
        </p>
        <p class="hero__signature rise" style="--rise-delay: 320ms">Criamos marcas de <em>Otto Patamar</em>.</p>
        <div class="hero__actions rise" style="--rise-delay: 400ms">
          <a class="btn btn-primary" href={DIAGNOSTIC_URL}>Começar pelo diagnóstico</a>
          <a class="link-arrow hero__whats" href={WHATSAPP_URL} target="_blank" rel="noopener noreferrer">
            <span class="link-arrow__icon" aria-hidden="true">→</span>
            <span class="link-arrow__label">Falar no WhatsApp</span>
          </a>
        </div>
      </div>
    </div>
  </section>

  <!-- ========================== SOLUÇÕES ========================== -->
  <section id="solucoes" class="section solucoes" data-surface="light">
    <div class="container">
      <div class="section-head">
        <p class="eyebrow reveal">Soluções</p>
        <h2 class="section-title reveal">
          Tudo que sua marca precisa para <span class="t-em">sair do lugar comum</span>.
        </h2>
      </div>

      <ul class="solucoes__list reveal-group">
        {solucoes.map((item, i) => (
          <li class="solucao reveal" style={`--reveal-index: ${i}`}>
            <div class="solucao__head">
              <span class="solucao__arrow" aria-hidden="true">→</span>
              <h3 class="solucao__name">{item.title}</h3>
            </div>
            <p class="solucao__desc">{item.desc}</p>
          </li>
        ))}
      </ul>

      <p class="solucoes__cta reveal">
        Pronto para começar?
        <a class="link-arrow" href={WHATSAPP_URL} target="_blank" rel="noopener noreferrer">
          <span class="link-arrow__icon" aria-hidden="true">→</span>
          <span class="link-arrow__label">Fale com a gente</span>
        </a>
      </p>
    </div>
  </section>

  <!-- =========================== MÉTODO =========================== -->
  <section id="metodo" class="section metodo">
    <div class="container">
      <div class="section-head">
        <p class="eyebrow reveal">Método Otto</p>
        <h2 class="section-title reveal">
          Sem achismo. Um caminho claro, do diagnóstico ao <span class="t-em">patamar</span>.
        </h2>
        <p class="section-lead reveal">
          O Método Otto organiza a construção de marca em três patamares. Cada um
          resolve uma pergunta antes de passar para o próximo. Estratégia antes de
          estética, sempre.
        </p>
        <!-- TODO (conteúdo, não design): fechar nome e sigla do método (timebox 1h). -->
      </div>

      <ol class="patamares reveal-group">
        {patamares.map((p, i) => (
          <li class="patamar reveal" style={`--reveal-index: ${i}; --step: ${i}`}>
            <span class="patamar__n">{p.n}</span>
            <div class="patamar__body">
              <h3 class="patamar__name">Patamar {p.n.replace('0', '')}. {p.name}.</h3>
              <p class="patamar__text">{p.text}</p>
            </div>
          </li>
        ))}
      </ol>
    </div>
  </section>

  <!-- =========================== SOBRE ============================ -->
  <section id="sobre" class="section sobre" data-surface="light">
    <div class="container sobre__grid">
      <figure class="sobre__photo reveal" aria-label="Foto de Gabriel Araújo (a inserir)">
        <span class="sobre__photo-note">[Sua foto aqui]</span>
      </figure>
      <div class="sobre__text">
        <p class="eyebrow reveal">Sobre</p>
        <h2 class="section-title reveal">Nascemos para tirar marcas do <span class="t-em">lugar comum</span>.</h2>
        <div class="sobre__body reveal">
          <p>
            A Otto é dirigida por <strong>Gabriel Araújo</strong>, diretor de arte e
            estrategista de marca. Cinco anos construindo marcas que precisam sustentar
            mais valor do que o mercado espera delas.
          </p>
          <blockquote class="sobre__quote">
            Marca forte não é a mais criativa. É a que ocupa um espaço que nenhum
            concorrente consegue defender.
          </blockquote>
        </div>
      </div>
    </div>
  </section>

  <!-- =========================== CASES ============================ -->
  <section id="cases" class="section cases">
    <div class="container">
      <div class="cases__head">
        <div class="section-head cases__head-text">
          <p class="eyebrow reveal">Cases</p>
          <h2 class="section-title reveal">Marcas de <span class="t-em">Otto Patamar</span>.</h2>
        </div>
        <div class="cases__controls reveal" aria-hidden="true">
          <button class="cases__arrow-btn" data-cases-prev type="button" aria-label="Case anterior">
            <span aria-hidden="true">←</span>
          </button>
          <button class="cases__arrow-btn" data-cases-next type="button" aria-label="Próximo case">
            <span aria-hidden="true">→</span>
          </button>
        </div>
      </div>
    </div>

    <div class="cases__viewport">
      <div class="container">
        <ul class="cases__track" data-cases-track>
          {cases.map((c, i) => (
            <li class="case-slide">
              {c.model ? (
                <a class="case-card case-card--model" href={c.href}>
                  <div class="case-card__media">
                    <span class="case-card__badge">Exemplo de layout</span>
                  </div>
                  <div class="case-card__info">
                    <span class="case-card__cat">{c.cat}</span>
                    <h3 class="case-card__name">{c.name}</h3>
                    <span class="case-card__cta">Veja como um case aparece →</span>
                  </div>
                </a>
              ) : (
                <div class="case-card case-card--empty">
                  <div class="case-card__media"></div>
                  <div class="case-card__info">
                    <span class="case-card__cat">{c.cat}</span>
                    <h3 class="case-card__name">{c.name}</h3>
                    <span class="case-card__cta case-card__cta--muted">[Case a publicar]</span>
                  </div>
                </div>
              )}
            </li>
          ))}
        </ul>
      </div>
    </div>

    <div class="container">
      <p class="cases__note reveal">
        <strong>Nota (conteúdo, não layout):</strong> os cases reais entram quando
        houver resultado com número. Até lá, a estrutura está pronta — veja a
        <a href="/case-modelo">página de case modelo</a> para o formato de aprovação.
      </p>
    </div>
  </section>

  <!-- ========================= DEPOIMENTOS ======================== -->
  <section id="depoimentos" class="section depoimentos" data-surface="light">
    <div class="container">
      <div class="section-head">
        <p class="eyebrow reveal">Depoimentos</p>
        <h2 class="section-title reveal">Histórias reais, com <span class="t-em">resultado real</span>.</h2>
      </div>
    </div>

    <div class="marquee reveal" data-marquee>
      <div class="marquee__track">
        {[...depoimentos, ...depoimentos].map((_, i) => (
          <figure class="depoimento" aria-hidden={i >= depoimentos.length ? 'true' : undefined}>
            <blockquote>“[Depoimento a coletar]”</blockquote>
            <figcaption>
              <span class="depoimento__name">[Nome · Empresa]</span>
              <span class="depoimento__hint">peça citando o resultado, não o processo</span>
            </figcaption>
          </figure>
        ))}
      </div>
    </div>
  </section>

  <!-- ========================= CTA FINAL ========================== -->
  <section class="section cta-final">
    <FluxBand class="cta-final__flux" texture="otto-band" />
    <div class="cta-final__scrim" aria-hidden="true"></div>
    <div class="container cta-final__content">
      <h2 class="cta-final__headline reveal">
        <span class="cta-final__quiet">Sua marca está sendo</span>
        <span class="cta-final__accent"><em>escolhida</em> ou apenas comparada?</span>
      </h2>
      <p class="cta-final__sub reveal">
        O diagnóstico mostra onde ela perde valor e o que precisa mudar.
      </p>
      <a class="btn btn-primary cta-final__btn reveal" href={DIAGNOSTIC_URL}>
        Começar pelo diagnóstico
      </a>
    </div>
  </section>
</BaseLayout>

<script>
  // Carrossel de cases: setas rolam a trilha por um card. Sem biblioteca.
  const track = document.querySelector<HTMLElement>('[data-cases-track]');
  const prev = document.querySelector<HTMLButtonElement>('[data-cases-prev]');
  const next = document.querySelector<HTMLButtonElement>('[data-cases-next]');

  if (track && prev && next) {
    const reduce = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
    const stepSize = () => {
      const slide = track.querySelector<HTMLElement>('.case-slide');
      if (!slide) return 320;
      const gap = parseFloat(getComputedStyle(track).columnGap || '24') || 24;
      return slide.getBoundingClientRect().width + gap;
    };
    const go = (dir: number) =>
      track.scrollBy({ left: dir * stepSize(), behavior: reduce ? 'auto' : 'smooth' });

    prev.addEventListener('click', () => go(-1));
    next.addEventListener('click', () => go(1));

    // desabilita a seta quando não há para onde ir
    const sync = () => {
      const max = track.scrollWidth - track.clientWidth - 2;
      prev.disabled = track.scrollLeft <= 2;
      next.disabled = track.scrollLeft >= max;
    };
    track.addEventListener('scroll', sync, { passive: true });
    window.addEventListener('resize', sync);
    sync();
  }
</script>

<style>
  /* ============================ HERO ============================ */
  .hero {
    position: relative;
    display: flex;
    align-items: center;
    min-height: min(88vh, 820px);
    padding-block: var(--space-8);
    background-color: var(--otto-ink);
    color: var(--otto-offwhite);
    overflow: hidden;
  }

  .hero__scrim {
    position: absolute;
    inset: 0;
    z-index: 1;
    /* vídeo já está a ~20% sobre o preto da marca, então o scrim é leve:
       só um respiro no topo (sob o header) e a fusão com a seção seguinte */
    background: linear-gradient(
      to bottom,
      rgb(var(--otto-ink-rgb) / 0.35),
      transparent 18%,
      transparent 72%,
      rgb(var(--otto-ink-rgb) / 0.85)
    );
  }

  .hero__container {
    position: relative;
    z-index: 2;
    width: 100%;
  }

  .hero__content {
    max-width: 46rem;
    margin-inline: auto;
    text-align: center;
  }

  .hero__headline {
    margin-top: var(--space-4);
    font-size: clamp(2.25rem, 4.2vw + 0.9rem, 3.5rem);
    letter-spacing: var(--tracking-tight);
    line-height: 1.06;
    text-wrap: balance;
  }

  /* diagramação: o "problema" em peso leve e tom apagado; a "resposta" em
     Pogonia Black, com a palavra-chave em vermelho. Contraste de peso e cor. */
  .hero__headline-quiet {
    display: block;
    font-weight: 400;
    color: rgb(var(--otto-offwhite-rgb) / 0.72);
  }

  .hero__headline-accent {
    display: block;
    margin-top: 0.12em;
    font-weight: 900;
    color: var(--otto-offwhite);
    letter-spacing: var(--tracking-tighter);
  }

  .hero__headline-accent em {
    font-style: normal;
    color: var(--otto-red);
  }

  .hero__descritor {
    margin: var(--space-5) auto 0;
    max-width: 36rem;
    font-size: var(--text-md);
    line-height: var(--leading-relaxed);
    color: rgb(var(--otto-offwhite-rgb) / 0.8);
  }

  .hero__signature {
    margin-top: var(--space-5);
    font-family: var(--font-display);
    font-size: var(--text-lg);
    letter-spacing: var(--tracking-tight);
  }

  .hero__signature em {
    font-style: normal;
    font-weight: 900;
    color: var(--otto-red);
  }

  .hero__actions {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    justify-content: center;
    gap: var(--space-5);
    margin-top: var(--space-6);
  }

  .hero__whats {
    color: var(--otto-offwhite);
  }

  /* ===================== CABEÇALHOS DE SEÇÃO ==================== */
  .section-head {
    max-width: 40rem;
    margin-bottom: var(--space-8);
  }

  .section-title {
    margin-top: var(--space-4);
    font-size: var(--text-xl);
    letter-spacing: var(--tracking-tight);
    line-height: var(--leading-snug);
    text-wrap: balance;
  }

  .section-lead {
    margin-top: var(--space-5);
    max-width: 38rem;
    font-size: var(--text-md);
    line-height: var(--leading-relaxed);
    color: var(--color-text-secondary);
  }

  /* ========================== SOLUÇÕES ========================= */
  .solucoes__list {
    list-style: none;
    margin: 0;
    padding: 0;
    border-top: 1px solid var(--color-border);
  }

  .solucao {
    display: grid;
    grid-template-columns: minmax(14rem, 1fr) 1.5fr;
    gap: var(--space-5) var(--space-8);
    padding-block: var(--space-6);
    border-bottom: 1px solid var(--color-border);
    transition: background-color var(--duration-base) var(--ease-out);
  }

  .solucao__head {
    display: flex;
    align-items: baseline;
    gap: var(--space-3);
  }

  .solucao__arrow {
    color: var(--otto-red);
    font-size: var(--text-md);
    transition: transform var(--duration-base) var(--ease-out);
  }

  .solucao__name {
    font-family: var(--font-display);
    font-size: var(--text-lg);
    letter-spacing: var(--tracking-tight);
    line-height: var(--leading-snug);
  }

  .solucao__desc {
    font-size: var(--text-base);
    line-height: var(--leading-relaxed);
    color: var(--color-text-secondary);
    max-width: 46ch;
  }

  .solucao:hover .solucao__arrow {
    transform: translateX(6px);
  }

  .solucoes__cta {
    margin-top: var(--space-7);
    font-size: var(--text-md);
    color: var(--color-text-secondary);
    display: inline-flex;
    flex-wrap: wrap;
    gap: var(--space-3);
    align-items: baseline;
  }

  /* =========================== MÉTODO ========================== */
  .metodo {
    background-color: var(--otto-charcoal);
    color: var(--otto-offwhite);
  }

  .patamares {
    list-style: none;
    margin: 0;
    padding: 0;
    display: grid;
    gap: var(--space-5);
  }

  .patamar {
    display: flex;
    gap: var(--space-5);
    padding: var(--space-6);
    margin-left: calc(var(--step) * clamp(0rem, 4vw, 3rem));
    border: 1px solid rgb(var(--otto-offwhite-rgb) / 0.12);
    border-radius: var(--radius-md);
    background: linear-gradient(135deg, rgb(var(--otto-offwhite-rgb) / 0.04), transparent 60%);
    transition: border-color var(--duration-base) var(--ease-out),
      transform var(--duration-base) var(--ease-out);
    will-change: transform;
  }

  .patamar:hover {
    transform: translateY(-4px);
    border-color: color-mix(in srgb, var(--otto-red) 55%, transparent);
  }

  .patamar__n {
    font-family: var(--font-display);
    font-weight: 900;
    font-size: var(--text-xl);
    line-height: 1;
    color: var(--otto-red);
    flex-shrink: 0;
  }

  .patamar__name {
    font-size: var(--text-lg);
    letter-spacing: var(--tracking-tight);
  }

  .patamar__text {
    margin-top: var(--space-2);
    max-width: 44ch;
    color: rgb(var(--otto-offwhite-rgb) / 0.74);
  }

  /* =========================== SOBRE =========================== */
  .sobre__grid {
    display: grid;
    grid-template-columns: 0.85fr 1.15fr;
    gap: var(--space-8);
    align-items: center;
  }

  .sobre__photo {
    margin: 0;
    aspect-ratio: 4 / 5;
    display: grid;
    place-items: center;
    border-radius: var(--radius-md);
    border: 1px dashed color-mix(in srgb, var(--color-text-secondary) 55%, transparent);
    background:
      radial-gradient(90% 70% at 50% 15%, rgb(var(--otto-red-rgb) / 0.06), transparent 60%),
      linear-gradient(160deg, rgb(36 36 36 / 0.05), rgb(36 36 36 / 0.02));
    color: var(--color-text-secondary);
  }

  .sobre__photo-note {
    font-size: var(--text-sm);
    letter-spacing: var(--tracking-wide);
    text-transform: uppercase;
  }

  .sobre__body {
    margin-top: var(--space-5);
  }

  .sobre__body p {
    font-size: var(--text-md);
    line-height: var(--leading-relaxed);
  }

  .sobre__quote {
    margin: var(--space-6) 0 0;
    padding-left: var(--space-5);
    border-left: 2px solid var(--otto-red);
    font-family: var(--font-display);
    font-size: var(--text-lg);
    line-height: var(--leading-snug);
    letter-spacing: var(--tracking-tight);
  }

  /* =========================== CASES =========================== */
  .cases {
    background-color: var(--otto-charcoal);
    color: var(--otto-offwhite);
    overflow: hidden;
  }

  .cases__head {
    display: flex;
    align-items: flex-end;
    justify-content: space-between;
    gap: var(--space-5);
    margin-bottom: var(--space-7);
  }

  .cases__head-text {
    margin-bottom: 0;
  }

  .cases__controls {
    display: flex;
    gap: var(--space-3);
    flex-shrink: 0;
  }

  .cases__arrow-btn {
    width: 3rem;
    height: 3rem;
    border-radius: var(--radius-full);
    border: 1px solid rgb(var(--otto-offwhite-rgb) / 0.22);
    background: transparent;
    color: var(--otto-offwhite);
    font-size: var(--text-md);
    cursor: pointer;
    display: grid;
    place-items: center;
    transition: background-color var(--duration-fast) var(--ease-standard),
      border-color var(--duration-fast) var(--ease-standard),
      opacity var(--duration-fast) var(--ease-standard);
  }

  .cases__arrow-btn:hover {
    background-color: var(--otto-red);
    border-color: var(--otto-red);
  }

  .cases__arrow-btn:disabled {
    opacity: 0.3;
    cursor: default;
  }

  /* viewport ocupa a largura total; a trilha usa o gutter do container para
     alinhar o primeiro card, e rola horizontalmente com snap */
  .cases__viewport {
    margin-bottom: var(--space-7);
  }

  .cases__track {
    list-style: none;
    margin: 0;
    padding: 0;
    display: flex;
    gap: var(--space-5);
    overflow-x: auto;
    scroll-snap-type: x mandatory;
    scroll-padding-left: 0;
    -webkit-overflow-scrolling: touch;
    scrollbar-width: none;
    /* sangra o overflow para as laterais sem cortar sombras */
    padding-bottom: var(--space-2);
  }

  .cases__track::-webkit-scrollbar {
    display: none;
  }

  .case-slide {
    scroll-snap-align: start;
    flex: 0 0 clamp(17rem, 32vw, 22rem);
  }

  .case-card {
    display: flex;
    flex-direction: column;
    height: 100%;
    border-radius: var(--radius-md);
    overflow: hidden;
    text-decoration: none;
    color: inherit;
    border: 1px solid rgb(var(--otto-offwhite-rgb) / 0.12);
    background-color: rgb(var(--otto-offwhite-rgb) / 0.02);
  }

  .case-card__media {
    position: relative;
    aspect-ratio: 4 / 3;
    background:
      radial-gradient(120% 120% at 72% 18%, rgb(var(--otto-red-rgb) / 0.22), transparent 58%),
      linear-gradient(155deg, #313131, var(--otto-ink));
  }

  .case-card__badge {
    position: absolute;
    top: var(--space-4);
    left: var(--space-4);
    font-size: var(--text-xs);
    font-weight: 500;
    letter-spacing: var(--tracking-wide);
    text-transform: uppercase;
    background-color: var(--otto-red);
    color: var(--otto-offwhite);
    padding: 0.2em 0.6em;
    border-radius: var(--radius-full);
  }

  .case-card__info {
    padding: var(--space-5);
    display: flex;
    flex-direction: column;
    gap: var(--space-2);
    flex-grow: 1;
  }

  .case-card__cat {
    font-size: var(--text-xs);
    letter-spacing: var(--tracking-wide);
    text-transform: uppercase;
    color: var(--color-text-secondary);
  }

  .case-card__name {
    font-family: var(--font-display);
    font-size: var(--text-lg);
    letter-spacing: var(--tracking-tight);
  }

  .case-card__cta {
    margin-top: auto;
    padding-top: var(--space-3);
    font-size: var(--text-sm);
    font-weight: 500;
    color: var(--otto-red-text-on-dark, #f0575a);
  }

  .case-card__cta--muted {
    color: var(--color-text-secondary);
  }

  .case-card--model {
    transition: transform var(--duration-base) var(--ease-out),
      border-color var(--duration-base) var(--ease-out);
    will-change: transform;
  }

  .case-card--model:hover {
    transform: translateY(-6px);
    border-color: var(--otto-red);
  }

  .case-card--empty {
    border-style: dashed;
  }

  .case-card--empty .case-card__media {
    opacity: 0.5;
  }

  .cases__note {
    max-width: 46rem;
    font-size: var(--text-sm);
    color: rgb(var(--otto-offwhite-rgb) / 0.7);
  }

  .cases__note a {
    color: var(--otto-red-text-on-dark, #f0575a);
  }

  /* ======================== DEPOIMENTOS ======================== */
  .marquee {
    margin-top: var(--space-4);
    overflow: hidden;
    -webkit-mask-image: linear-gradient(90deg, transparent, #000 8%, #000 92%, transparent);
    mask-image: linear-gradient(90deg, transparent, #000 8%, #000 92%, transparent);
  }

  .marquee__track {
    display: flex;
    gap: var(--space-5);
    width: max-content;
    animation: marquee 48s linear infinite;
  }

  .marquee:hover .marquee__track {
    animation-play-state: paused;
  }

  @keyframes marquee {
    from { transform: translate3d(0, 0, 0); }
    to { transform: translate3d(-50%, 0, 0); }
  }

  .depoimento {
    flex: 0 0 clamp(18rem, 30vw, 24rem);
    margin: 0;
    padding: var(--space-6);
    border: 1px dashed var(--color-border);
    border-radius: var(--radius-md);
    background-color: rgb(var(--otto-charcoal-rgb) / 0.02);
    display: flex;
    flex-direction: column;
    gap: var(--space-4);
  }

  .depoimento blockquote {
    margin: 0;
    font-family: var(--font-display);
    font-size: var(--text-lg);
    letter-spacing: var(--tracking-tight);
    line-height: var(--leading-snug);
    color: var(--color-text-secondary);
  }

  .depoimento figcaption {
    display: flex;
    flex-direction: column;
    gap: var(--space-1);
    font-size: var(--text-sm);
  }

  .depoimento__name {
    font-weight: 500;
    color: var(--color-text);
  }

  .depoimento__hint {
    font-size: var(--text-xs);
    color: var(--color-text-secondary);
  }

  /* ========================= CTA FINAL ========================= */
  .cta-final {
    position: relative;
    overflow: hidden;
    background-color: var(--otto-ink);
    color: var(--otto-offwhite);
    text-align: center;
  }

  .cta-final__scrim {
    position: absolute;
    inset: 0;
    z-index: 1;
    background: radial-gradient(
      90% 130% at 50% 40%,
      rgb(var(--otto-ink-rgb) / 0.55),
      rgb(var(--otto-ink-rgb) / 0.92) 75%
    );
  }

  .cta-final__content {
    position: relative;
    z-index: 2;
    max-width: 40rem;
    margin-inline: auto;
  }

  .cta-final__headline {
    font-size: var(--text-2xl);
    letter-spacing: var(--tracking-tighter);
    line-height: 1.06;
    text-wrap: balance;
  }

  /* mesma diagramação do hero: linha leve/apagada → linha Black com
     a palavra-chave em vermelho */
  .cta-final__quiet {
    display: block;
    font-weight: 400;
    color: rgb(var(--otto-offwhite-rgb) / 0.72);
  }

  .cta-final__accent {
    display: block;
    margin-top: 0.12em;
    font-weight: 900;
    color: var(--otto-offwhite);
  }

  .cta-final__accent em {
    font-style: normal;
    color: var(--otto-red);
  }

  .cta-final__sub {
    margin-top: var(--space-5);
    font-size: var(--text-md);
    color: rgb(var(--otto-offwhite-rgb) / 0.78);
  }

  .cta-final__btn {
    margin-top: var(--space-7);
  }

  /* ========================= RESPONSIVO ======================== */
  @media (max-width: 60rem) {
    .sobre__grid {
      grid-template-columns: 1fr;
      gap: var(--space-6);
    }
    .sobre__photo {
      aspect-ratio: 3 / 2;
      max-height: 22rem;
    }
  }

  @media (max-width: 48rem) {
    .solucao {
      grid-template-columns: 1fr;
      gap: var(--space-3);
      padding-block: var(--space-5);
    }
  }

  @media (max-width: 40rem) {
    .patamar {
      margin-left: 0;
      padding: var(--space-5);
    }
    .cases__head {
      flex-direction: column;
      align-items: flex-start;
    }
  }

  /* Movimento reduzido: para a esteira e deixa rolar manualmente */
  @media (prefers-reduced-motion: reduce) {
    .marquee__track {
      animation: none;
    }
    .marquee {
      overflow-x: auto;
    }
  }
</style>
